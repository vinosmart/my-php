{
  "build": {
    "command": "composer install --no-dev",
    "watch": ["composer.json", "composer.lock"]
  },
  "start": {
    "command": "echo \"Using port: ${PORT}\" && php -d display_errors=1 -S 0.0.0.0:${PORT} -t public"
  }
}
